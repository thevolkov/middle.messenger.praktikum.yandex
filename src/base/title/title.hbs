<div class="title-template">
  <h1 class="title align-center {{class}}">{{text}}</h1>

  {{#if subtitle}}
    <p class="subtitle">{{subtitle}}</p>
  {{/if}}

  {{#if code}}
    <span class="title__error__code">{{code}}</span>
  {{/if}}

</div>
